import React from 'react'
import ReactDOM from 'react-dom/client'
import SearchGiftApp from './SearchGiftApp.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SearchGiftApp />
  </React.StrictMode>,
)
